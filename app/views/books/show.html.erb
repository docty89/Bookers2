<div class="post-body">
  <%= attachment_image_tag @book.user, :profile_image, :fill, 60, 60 %>
  <p>Title：<%= @book.title %></p>
  <p>Opinion：<%= @book.body %></p>
  <p>ユーザーid：<%= @book.user_id %></p>
  <% if @book.user == current_user %>
    <%= link_to "Destroy", book_path(@book), method: :delete %>
  <% end %>
</div>